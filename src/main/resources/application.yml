server:
  port: 7070
spring:
  datasource:
    password: root
    url: jdbc:mysql://localhost:3306/federation3?createDatabaseIfNotExist=true
    username: root
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: false
    #   désactive la validation hibernate lors de la persistence
    properties:
      javax:
        persistence:
          validation:
            mode: none
    #  security:
#    user:
#      name: admin
#      password: admin
multipart:
   maxFileSize:10MB


